# ============================================================
# foultsPredictor — unified dependencies
# ============================================================

# --- features_generator ---
requests>=2.32.0
supabase>=2.10.0
python-dotenv>=1.1.0
beautifulsoup4>=4.12.3
undetected-chromedriver>=3.5.5

# --- prediction_models ---
torch>=2.6
statsmodels>=0.14.4
pgmpy>=0.1.26,<1.0.0  # 1.0.0 tiene breaking changes — migrar antes de subir
seaborn>=0.13.2

# --- shared ---
numpy>=2.0
pandas>=2.2,<3.0  # 3.0 tiene breaking changes (CoW obligatorio) — migrar antes de subir
pyarrow>=18.0
scipy>=1.13
scikit-learn>=1.5
pyyaml>=6.0.2
matplotlib>=3.9
tqdm>=4.67
